<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace: SocietyPass.Mobile.Core.Controls;assembly= SocietyPass.Mobile.Core"
             x:Class="SocietyPass.Mobile.Core.Views.StepperView">
  <ContentView.Content>
      <StackLayout Orientation="Horizontal">
          <Button Image="minus" WidthRequest="30" HeightRequest="15" Clicked="MinusButton_OnClicked"
                               BackgroundColor="Transparent" />
          <Grid VerticalOptions="Center">
              <Frame CornerRadius="8" BackgroundColor="{StaticResource GrayLight}"  HasShadow="False"
                     OutlineColor="Transparent" WidthRequest="30" />
              <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                  <Label TextColor="Black" x:Name="StepLabel" FontSize="15" />
              </StackLayout>
          </Grid>
          <Button Image="plus" WidthRequest="30" HeightRequest="15" BackgroundColor="Transparent"
                               Clicked="PlusButton_OnClicked" />
      </StackLayout>
    </ContentView.Content>
</ContentView>